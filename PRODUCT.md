# Agent Micro

Codex CLI를 손끝으로 다루는 플로팅 맥로패드.

데스크 위에 항상 떠 있는 작은 컨트롤러로, 에이전트 선택·승인·음성·단축 액션을 키보드와 같은 감각으로 조작합니다.

---

## 이런 분께

- Codex CLI를 여러 창·스플릿으로 동시에 쓰는 분
- 터미널을 떠나지 않고 Approve / Decline / Fork를 빠르게 누르고 싶은 분
- 말로 지시하고, 결과는 그대로 CLI에 보내고 싶은 분

---

## 시작하기

### 1. 앱 실행

배포된 **Agent Micro** 앱을 실행합니다.  
메뉴바 왼쪽 상단에 앱 이름이 보이면 정상입니다.

### 2. Codex 연결

1. 제목 옆 **점** 또는 **↻** 을 누릅니다.
2. 브라우저에서 ChatGPT / Codex 로그인을 완료합니다.
3. 점이 **초록색**이면 연결됨 · **노란색**이면 아직 demo(미연결)입니다.

> Shift + 점 = 강제 로그인(브라우저를 다시 띄움)

### 3. 마이크용 API 키 (선택)

연결 직후 Whisper용 **OpenAI Platform API 키(`sk-…`)** 설정을 안내합니다.

| 선택 | 결과 |
|------|------|
| 저장하고 완료 | 패드 마이크로 음성 → 텍스트 → 현재 에이전트에 전송 |
| 나중에 | 음성 없이 키·조이스틱만 사용 |

---

## macOS 권한

처음 쓸 때 아래 권한을 허용해 주세요.

| 권한 | 용도 |
|------|------|
| **마이크** | 음성 인식(Whisper) |
| **손쉬운 사용(Accessibility)** | CLI 창 포커스·스플릿·데스크톱 단축키 |

시스템 설정 → 개인정보 보호 및 보안 → 해당 항목 → **Agent Micro** 허용

---

## 패드 구성

### 에이전트 슬롯 (1–6)

최대 6개의 Codex CLI 세션을 슬롯에 둡니다.

| 슬롯 | 동작 |
|------|------|
| **1** | 새 터미널 창에서 Codex 시작 |
| **2–6** | **직전 슬롯** 창에서 순서대로 스플릿 (1→2→3…) |
| 이미 있는 슬롯 | 해당 패인으로 포커스 |

앞 번호가 없으면 뒤 번호는 열리지 않습니다. (예: 1 없이 2만 누르면 안내 후 무시)

### 하단 키

| 키 | 이름 | 동작 |
|----|------|------|
| ⚡ | Fast | reasoning을 minimal로 |
| ✓ | Approve | 승인 |
| ✕ | Decline | 거절 |
| ⑂ | Fork | 현재 스레드 분기 → 다음 빈 슬롯 + 스플릿 (슬롯이 가득 차면 비활성) |
| 🎙 | Mic | 음성 입력 |
| ✦ | Send | Continue · 더블탭 = 새 채팅 |

키캡 **우클릭**으로 아이콘을 바꿀 수 있습니다. (표시만 변경 · 동작은 유지)

### Dial

돌리면 reasoning 강도: **minimal → low → medium → high → xhigh**

### Touch · 레이어

탭할 때마다 레이어가 바뀝니다.

**Codex → Prompts → App**

조이스틱 방향 액션이 레이어마다 달라집니다.

| 레이어 | ↑ | → | ↓ | ← |
|--------|---|---|---|---|
| **Codex** | Plan | 히스토리 → | 새 채팅 | 히스토리 ← |
| **Prompts** | review PR | debug | docs | refactor |
| **App** | composer | new chat | sidebar | history ← |

---

## 음성 (Mic)

API 키를 저장한 뒤 사용합니다.

| 조작 | 동작 |
|------|------|
| **홀드** | 말하는 동안 녹음 (PTT) |
| **더블탭** | hands-free (다시 누르면 종료·전송) |
| **단축키 + D** | 녹음 토글 |

인식된 텍스트는 **현재 선택된 에이전트**로 전송됩니다.

---

## 단축키

기본 수정키는 **⇧ Shift**입니다.  
앱 안 **키보드** 아이콘 → 키 맵핑에서 **⇧ Shift** / **⌘ Command** 로 바꿀 수 있습니다.

아래는 수정키를 `Mod`로 표기합니다. (기본 = ⇧)

| 단축키 | 동작 |
|--------|------|
| **⌘⇧M** | 창 숨기기 / 보이기 (전역) |
| **⌘⇧Q** | 종료 |
| **Mod + 1–6** | 에이전트 슬롯 |
| **Mod + Q W E R D F** | Fast · Approve · Decline · Fork · Mic · Send |
| **Mod + Tab** | Touch · 레이어 전환 |
| **Mod + ↑ ↓ ← →** | 조이스틱 |

단축키는 **패드 창** 또는 이 패드로 연 **CLI 터미널**이 앞에 있을 때 동작합니다.

---

## 앱 안 메뉴

| 버튼 | 내용 |
|------|------|
| **?** | 사용 설명서 |
| **키보드** | 키 맵핑 · 수정키 선택 |
| **톱니** | Codex 설정 (sandbox, 승인 정책, 타임아웃, `.codexignore` 등) |
| **↻ / 점** | Connect · Reconnect |

설정은 `~/.codex` 에 저장되며, **새로 연 CLI 창**부터 적용됩니다.

---

## 상태 표시

| 표시 | 의미 |
|------|------|
| 초록 점 | Codex CLI 연결됨 |
| 노란 점 | demo · 아직 미연결 |
| 에이전트 LED | 슬롯별 idle / thinking / complete / input / error |

하단 HUD에 현재 에이전트·레이어·reasoning이 표시됩니다.

---

## 요구 사항

- macOS
- [Codex CLI](https://github.com/openai/codex) 사용 가능한 환경 (ChatGPT 로그인)
- 음성 사용 시 OpenAI Platform API 키 (`sk-…`)
- 터미널 앱 (Ghostty 등 · 스플릿/포커스에 사용)

무료로 **7일** 사용할 수 있습니다. 기간이 끝나면 앱이 잠기고, **Gumroad 구매 후 받은 라이선스 키**로 다시 켤 수 있습니다.

---

## 팁

1. 에이전트는 **1번부터 순서대로** 여는 것이 가장 안정적입니다.
2. Fork는 슬롯이 비어 있을 때만 새 분기를 만듭니다. 6개 모두 찼으면 키를 쓸 수 없습니다.
3. 마이크가 안 되면 시스템 설정에서 마이크·손쉬운 사용 권한을 다시 확인하세요.
4. Connect 후에도 점이 노라면 **↻** 으로 다시 연결하거나 Shift+점으로 로그인을 다시 해 보세요.

---

Agent Micro — Codex CLI를 손끝에.
