{
  "name": "@agent-micro/landing",
  "version": "1.2.0",
  "private": true,
  "description": "Agent Micro product landing page",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && python3 -m http.server 4173 --directory dist",
    "preview": "python3 -m http.server 4173 --directory dist"
  }
}
